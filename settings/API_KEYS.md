# API Key Configuration

This application requires a Google Gemini API key to function.

**IMPORTANT**: The API key is configured **exclusively** through an environment variable named `API_KEY`.

- **Do not** paste your API key here.
- This file is for informational purposes only.
- To set up the application, ensure the `API_KEY` environment variable is available in the execution environment where this app is running.
